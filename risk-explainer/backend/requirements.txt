# Pinned to versions verified against Python 3.11–3.14.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
SQLAlchemy==2.0.52
openai==3.10.0
python-dotenv==1.2.3
pytest==9.1.1
httpx==0.28.1

# Postgres driver — only needed when DATABASE_URL points at Postgres
# (deployment). Harmless to have installed for local SQLite dev.
psycopg[binary]>=3.2,<4
